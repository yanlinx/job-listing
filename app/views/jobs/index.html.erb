<!-- <div class="col-md-12"> -->
    <!-- <div class="pull-right"> -->
         <!-- <%= link_to("New Job", new_job_path, :class => "btn btn-default" ) %> -->
    <!-- </div> -->
<div class="dropdown clearfix pull-right">
  <button class="btn btn-default dropdown-toggle" type="button" id="dropdownMenuDriver" data-toggle="dropdown" aria-haspopup="true" aria-expanded="true">
    排序
    <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" aria-labelledby="dropdownMenuDrivier">
    <li><%= link_to("按照薪资下限排序",jobs_path(:order =>"by_lower_bound"))%></li>
    <li><%= link_to("按照薪资上限排序",jobs_path(:order =>"by_upper_bound"))%></li>
    <li><%= link_to("按照发表时间排序",jobs_path)%></li>
    </li>
  </ul>
</div>

    <table class="table table-hover">
      <br>
      <br>
      <thead>
        <tr>
          <th>Job</th>
          <th>Description</th>
          <th>Upper wage</th>
          <th>Lower wage</th>
          <th>Status</th>
          <th>Lately</th>
      </thead>
        <tbody>
            <% @jobs.each do |job|%>
            <tr>
                <td><%= link_to(job.title,job_path(job))%></td>
                <td><%= job.description %></td>
                <td><%= job.wage_upper_bound %></td>
                <td><%= job.wage_lower_bound %></td>
                <td>
                    <%= link_to("Edit",edit_job_path(job))%>
                    |
                    <%= link_to("Delete",job_path(job),method: :delete,data:{confirm: "Are you sure"} )%>
                </td>
                <td><%= job.created_at %></td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
